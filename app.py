# -*- coding: utf-8 -*-
import os
import streamlit as st
from google import genai
from google.genai import types

st.title("馬柱 ＆ 予想支援アプリ")
st.write("競馬新聞や出馬表の画像を複数枚（全体像＋拡大・別角度など）アップロードできます。AIが情報を総合して厳格にスコア化します。")

# StreamlitのSecretsから自動取得。ダメならサイドバーの入力欄を表示
try:
    api_key = st.secrets["GEMINI_API_KEY"]
except Exception:
    api_key = st.sidebar.text_input("Gemini APIキーを入力してください", type="password")

if api_key:
    try:
        client = genai.Client(api_key=api_key)

        # 複数ファイルのアップロードに対応 (accept_multiple_files=True)
        uploaded_files = st.file_uploader(
            "馬柱の画像を選択してください（複数選択可：全体＋拡大など）", 
            type=["jpg", "jpeg", "png"], 
            accept_multiple_files=True
        )

        if uploaded_files:
            # アップロードされたすべての画像を画面に表示
            for uploaded_file in uploaded_files:
                st.image(uploaded_file, caption=uploaded_file.name, use_container_width=True)
            
            if st.button("この馬柱を解析してスコア化する"):
                with st.spinner("Analyzing multiple images..."):
                    
                    # 複数画像のバイトデータをリストに格納
                    content_parts = []
                    for uploaded_file in uploaded_files:
                        image_bytes = uploaded_file.getvalue()
                        content_parts.append(
                            types.Part.from_bytes(
                                data=image_bytes,
                                mime_type=uploaded_file.type,
                            )
                        )
                    
                    # 小数点第一位対応 ＆ ハレーション・捏造防止 ＆ 複数画像参照の最強プロンプト
                    prompt = """
# 役割 (Persona)
あなたはプロの競馬アナリストであり、感情を排してデータと客観的事実に基づき出走馬を数値化・評価するAIです。
提供された**複数の出走表（馬柱）画像（全体像やハレーション対策の拡大写真など）を相互に参照し**、情報を補完し合って正確なデータを読み取ってください。データや事実に基づかない情報の捏造（ハルシネーション）を厳格に禁止します。

# 採点・評価基準 (100点満点・小数点第一位まで算出)
画像から読み取ったデータをもとに、以下の7項目・合計100点満点（**※スコアは必ず「85.5点」のように小数点第一位まで細かく算出してください**）で各馬を厳密に評価してください。

1. ① 近走能力 (20点満点)
   - 着順だけでなく、内容（直線での不利、大外ぶん回し、ハイペース先行などの不利・恩恵）を精査して加減点します。
   - 着差の目安: 0.0〜0.2秒（高評価）、0.3〜0.5秒（良）、0.6〜1.0秒（普通）、1.1〜1.5秒（やや減点）、1.6秒以上（大幅減点）。

2. ② コース・距離・馬場適性 (18点満点)
   - コース実績（6点）：同コース・同競馬場・類似条件の好走実績。
   - 距離実績（6点）：今回の距離カテゴリーにおける実績。
   - 馬場実績（6点）：良馬場実績、または道悪実績＋血統の組み合わせ。

3. ③ 調教・追い切り (17点満点)
   - 時計の速さだけでなく、「全体時計は速いが終い失速」は減点、「全体は普通だが終い11秒台で鋭く伸びる」は高評価とします。最終・1週前・併せ馬・調教師コメント等も加味します。

4. ④ 脚質・展開 (15点満点)
   - 全馬の脚質（逃げ・先行・好位・差し・追込）を分類します。
   - 逃げ馬の頭数と想定ペース（S/M/H）による展開の利不利（例：Hペースなら差し・追込をプラス評価）を反映させます。

5. ⑤ 騎手 (12点満点)
   - 単なるネームバリューではなく、当該コース・距離の成績、および「乗り替わり」によるプラス・マイナスを評価します。

6. ⑥ 血統 (10点満点)
   - 基本は実績重視のため10点満点ですが、芝→ダート、短距離→中距離、良→重馬場などの条件変更時は血統の配分・重要度を上げます。

7. ⑦ 枠順・馬番 (8点満点)
   - 基本点4点に、コース形態ごとの影響度に応じた枠順補正（＋4〜－4点）を加算します。

# 処理手順 (Chain of Thought)
1. **データ抽出:** 複数枚の画像を見比べ、ハレーションによる読み間違いや行・列の勘違いを防ぎながら全頭の情報を正確に抽出します。
2. **多角採点:** 上記①〜⑦の基準に従い、各馬の点数をロジカルに算出し、**小数点第一位まで（例: 85.5点）**の合計点数を求めます。
3. **スコア順ソート:** 算出した合計点数が**高い順（降順）**に並べ替えてください。
4. **総合評価とランク付け:** 合計点に基づき判定を下します。
   - 90.0〜100.0点: S (◎本命候補)
   - 85.0〜89.9点: A+ (◎○候補)
   - 80.0〜84.9点: A (○有力)
   - 75.0〜79.9点: B+ (▲相手本線)
   - 70.0〜74.9点: B (△相手)
   - 65.0〜69.9点: C+ (△穴)
   - 60.0〜64.9点: C (◇抑え)
   - 59.9以下: D (基本消し)
5. **期待値（妙味）の考慮:** 「能力点数 × オッズ」の観点から期待値・妙味を考慮し、最終的な印を決定します。

# 出力フォーマット
以下の構成で結果を出力してください。

## 📊 総合評価ランキング（スコア順）
- 順位 / 馬番・馬名 / 合計スコア：**XX.X点**（判定ランク）
- 各馬の評価理由（近走内容、適性、調教、展開面のプラスマイナスを簡潔に）

## 🎯 最終推奨印と見解
- ◎ 本命: （理由・期待値の解説）
- ○ 対抗: 
- ▲ 単穴: 
- ☆ 穴馬: 
- △ 抑え: 
- ◇ 超穴: 

# 制約事項 (Guardrails)
* **ハルシネーション・捏造の厳禁:** 複数画像を照らし合わせ、不鮮明な部分は勝手に推測せず、画像内の事実のみに基づいて採点してください。
* **情報不足時の対応:** どうしても情報が不足している場合は、「画像が不鮮明なため、正しく採点できません。鮮明な拡大写真をもう一度アップロードしてください」と答えてください。
* **根拠の明示:** スコアの算出にあたっては、画像内のどの情報を根拠にしたのかを必ず示してください。
                    """

                    # プロンプトをリストの最後に追加
                    content_parts.append(prompt)

                    response = client.models.generate_content(
                        model='gemini-3.6-flash',
                        contents=content_parts
                    )
                    
                    st.subheader("📊 分析結果・予想一覧")
                    st.markdown(response.text)
                    
    except Exception as e:
        st.error(f"エラーが発生しました: {e}")
else:
    st.info("APIキーが設定されていません。")
